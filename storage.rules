rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Post images - anyone authenticated can upload, anyone can read
    match /posts/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
